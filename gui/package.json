{
  "name": "auto-shop-gui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "dependencies": {
    "drizzle-orm": "^0.38.0"
  },
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "lsof -ti :3400 | xargs kill -9 2>/dev/null; npm run dev --workspace=server",
    "dev:client": "lsof -ti :5473 | xargs kill -9 2>/dev/null; npm run dev --workspace=client",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "build:server": "npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",
    "qa:playwright": "playwright test tests/workflow-manual-qa.spec.js",
    "start": "npm run start --workspace=server",
    "db:generate": "npm run db:generate --workspace=server",
    "db:migrate": "npm run db:migrate --workspace=server",
    "db:push": "npm run db:push --workspace=server",
    "db:studio": "npm run db:studio --workspace=server"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
